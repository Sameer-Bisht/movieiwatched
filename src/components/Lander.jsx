import React from 'react';

const Lander = () => {
    return (
        <div>
            <button onClick={() => window.location.href = 'tamta'}>Saurav</button>
            <button onClick={() => window.location.href = 'bisht'}>Sameer</button>
        </div>
    );
};

export default Lander;